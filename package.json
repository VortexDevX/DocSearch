{
  "name": "doctor-search",
  "version": "2.0.0",
  "description": "Doctor Search Website",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup-db": "node database/setup.js",
    "build": "echo 'No build step required'",
    "postinstall": "npm run setup-db"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^16.3.1",
    "express": "^4.21.2",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
